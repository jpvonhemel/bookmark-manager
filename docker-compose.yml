services:
  bookmark-manager:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - bookmark-data:/app/data
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  bookmark-data:
